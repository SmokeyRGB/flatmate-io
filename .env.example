# Supabase project `flatmate-io` (cjinhzzvjryojvhngjjn), region eu-west-1 — confirmed via
# Supabase MCP `get_project`, 2026-09-16. Distinct from the solver's eu-central-1 AWS Lambda
# (ADR-005) — that's a separate service in a separate region, not a mismatch to fix.
#
# Transaction-mode Supavisor pooler (port 6543), per docs/adr/0006-*.md ("Änderung 2026-09-16")
# and research.md §1 — required because the app runs serverless/edge on Vercel and cannot hold a
# long-lived direct connection. Prepared statements MUST be disabled on the client
# (`postgres(connectionString, { prepare: false })`) or writes fail unpredictably.
#
# No real credentials committed here (G-A2) — replace [YOUR-PASSWORD] locally, never commit it.
DATABASE_URL="postgres://postgres.cjinhzzvjryojvhngjjn:[YOUR-PASSWORD]@aws-0-eu-west-1.pooler.supabase.com:6543/postgres"

# Supabase Auth (ADR-006) — from the project's API settings, not secret on their own but not
# committed with real values either, to keep this file a template.
NEXT_PUBLIC_SUPABASE_URL="https://cjinhzzvjryojvhngjjn.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[YOUR-ANON-KEY]"
